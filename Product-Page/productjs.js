// A function to toggle filter box 
function toggleFilterBox() {
            var filterBox = document.getElementById('filterBox');
            filterBox.style.display = (filterBox.style.display === 'none' || filterBox.style.display === '') ? 'block' : 'none';
}
